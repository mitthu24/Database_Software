# Infrastructure

Vercel: frontend and previews.
Railway: NestJS backend and PostgreSQL.
Firebase: authentication.
Brevo: transactional email.
Cloudflare: DNS, TLS, WAF, proxy/CDN.

Secrets are environment variables only. Keep .env.example free of credentials.
