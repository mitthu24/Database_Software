# UI/UX

Founder panel: sidebar + dashboard + companies list + company detail + administrator management + status controls + audit activity.

Company panel: workspace sidebar + table list + table structure view + create/edit dialogs + column management.

Do not build spreadsheet-style data entry.

Principles: simple, fast, accessible, desktop-first, clear empty states, clear errors and confirmations for destructive operations.
