# App Web Flow

Login → Firebase authentication → backend token verification → application role resolution → role-specific dashboard.

Super Admin → Companies → Company Detail → Admin/Status/Audit.

Company → Workspace → Tables → Table Structure → Table/Column operations.

Unauthorized access → safe 403/404 without leaking tenant information.
